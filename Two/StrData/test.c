#include <stdio.h>
#include <stdlib.h>

typedef struct BTreeNode
{
    int value;
    BTreeNode* left;
    BTreeNode* right;
    int h;
}BTreeNode;
// Вставка и балансировка В дерева
BTreeNode* CreateNode(int data)
{
    BTreeNode* node = (BTreeNode*)calloc()
}
BTreeNode* InsertNode(int data, BTreeNode* root)
{
    if(root == NULL)
    {

    }
}

int main()
{
    
}