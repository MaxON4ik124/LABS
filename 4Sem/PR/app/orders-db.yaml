apiVersion: v1
kind: Service
metadata:
  name: shop-db
  namespace: xxe-lab
spec:
  clusterIP: None
  selector:
    app: shop-db
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
