 // while(x.sign == 1)
    //     x = plus(x, Dpi);
    // while(higher(x, Dpi))
    //     x = minus(x, Dpi);
    // if(higher(x, pi))
    // {
    //     x = minus(x, pi);
    //     priv = 1;
    // }